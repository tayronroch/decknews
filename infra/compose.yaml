services:
  database:
    container_name: decknews-dev-postgres
    image: postgres:17-alpine
    env_file:
      - ../.env
    ports:
      - '5430:5432'
    volumes:
      - decknews_db_data:/var/lib/postgresql/data
    restart: always

volumes:
  decknews_db_data:
